//
// Created by User on 03.05.2020.
//

#include "header.h"

//t_entity *createNote(SDL_Renderer *renderer, char *texture) {
//    t_entity *note = malloc(sizeof(t_entity));
//    note->rect = malloc(sizeof(SDL_Rect));
//    note->rect->x = rand() % WINDOW_WIDTH;
//    note->rect->y = (rand() % (-100 + 1 + 3000) - 3000);
//    note->rect->w = NOTE_WIDTH;
//    note->rect->h = NOTE_HEIGHT;
//    note->texture = IMG_LoadTexture(renderer, texture);
//    int level = 0;
//    int score = 0;
//    note_falling(note, &level, &score);
//    return note;
//}
